# Discuss ActionPlan — กรกฎาคม 2569 (รายสัปดาห์)

> เริ่มโครงสร้างใหม่ตั้งแต่เดือนนี้ — 1 ไฟล์ต่อเดือน แบ่งเป็น sprint รายสัปดาห์ข้างใน แทนการแยกไฟล์ต่อสัปดาห์แบบเดิม (ดู `2026-07-06.md` สำหรับสัปดาห์ 30 มิ.ย.–6 ก.ค. ที่ publish ไปแล้วก่อนเปลี่ยนระบบ)
>
> ดึงจาก retrospective ทุก session ในแต่ละสัปดาห์ (ψ/memory/retrospectives) ไม่ใช่แค่เซสชันเดียว

## Week 2 (7–11 ก.ค. 2569)

### DAPP (iStudio — Laravel + React)

- `7 ก.ค.` **WhatsNew Manual Management** — หน้า admin ใหม่จัดลำดับ WhatsNew stack (drag reorder, add/remove), `p_whats_new_rank` เป็น ordering source of truth แทน release_date
- `7 ก.ค.` **Partner Service Cards + WhatsNew Accessories Tail** — stripe "ครบ จบ ทุกบริการ" + WhatsNew tail ต่อท้ายด้วยการ์ดอุปกรณ์เสริม (reuse LOB Collections)
- `7 ก.ค.` **อุปกรณ์เสริม Apple homepage grid** — CMS-editable ครบ 7/7 หมวด (Mac/iPad/iPhone/Watch/AirPods/TV/AirTag)
- `8 ก.ค.` **All-Accessories page ใหม่** — เปิดสินค้าที่มองไม่เห็นมาก่อน 70 รายการ 15 หมวด + แก้ route-order bug ที่เคยทำ `/pages/compare` พังเงียบๆ
- `8 ก.ค.` **Footer CMS + LOB Stripe CMS** — GlobalFooter link columns แก้ไขได้จาก admin, homepage "ผลิตภัณฑ์ Apple ทั้งหมด" stripe ผูกลิงก์ได้ครบ (แก้ bug เดิมที่ทุก tile ไม่มีลิงก์เลย)
- `8 ก.ค.` **2C2P v4.3 bugs + skill** — แก้ currencyCode (alpha↔numeric), QRCODE→QR, สร้าง skill reference เต็ม
- `8 ก.ค.` **Checkout Hardening Marathon** — เจอ+แก้ 13 บั๊ก end-to-end จริง (AppleCare+ พังทุก checkout, trade-in บวกราคาผิด, cart ไม่เคลียร์, CloudFront 403, address autocomplete เป็น stub 7 รายการ)
- `9 ก.ค.` **CMS Shadow DOM migration** — iframe html-mode content ย้ายไป Shadow DOM (แก้ bug header ซ้อนกัน)
- `9 ก.ค.` **Graper ถอดถอนสมบูรณ์** — ลบ vendor page builder ทั้ง 2 repo (backup ไว้ก่อน)
- `9 ก.ค.` **SEO Audit เต็มระบบ** — แก้ Sitelinks placeholder, Organization logo ว่าง
- `9 ก.ค.` **Zigzag CMS + Admin Help Guide** — homepage Zigzag section (3 slot คงที่) แก้ไขได้จาก admin, สร้างหน้าคู่มือการใช้งานใหม่สำหรับแอดมินที่หลงทาง
- `9 ก.ค.` **Order Flow Overhaul** — address lock-until-paid, Order Notes/Logs แบบ WooCommerce, สถานะ `shipped` ใหม่พร้อม tracking, manual-order payment detail (บัญชี/สลิป) — 13 งานย่อย
- `10 ก.ค.` **DAPP Hardening Marathon** — แก้ 6 บั๊ก production จริง (2 Filament AttachAction crash, 3 Shield policy หาย, cart event-storm ทำ 429 จริง), ปรับ rate limit 2 รอบ, ปิดช่องโหว่ SEO (admin panel ไม่มี noindex เลย), กู้ WhatsNew taglines ที่หายจาก DB incident

### Bolttech Service

- `7 ก.ค.` **Verification Report field-spec fix** — deploy จริง verify แล้วบน prod
- `7 ก.ค.` **Home Dashboard** — build ใหม่ + แก้ visual bug ตาม feedback

### FFD Portal (education.uficon.com)

- `7 ก.ค.` **Backlog scoping** — 4 งาน (auto-redirect, ปุ่มกลับ Portal, Catalog/Tag→Coupon, audit ref-tracker.php) สร้าง Lark task ครบ

### Dev Radar / Oracle Infra

- `7 ก.ค.` sync Oracle skills v3.0.4→v26.5.16
- `10 ก.ค.` ตั้ง CORE rule commit message ภาษาไทย 5 หัวข้อ

### งานด่วน/งานแทรก (Week 2)

- `9 ก.ค.` **⚠️ Production DB wipe จาก `php artisan test`** — `RefreshDatabase` ไม่มี `.env.testing` แยก ล้าง DB shared local+staging ทั้งหมด กู้คืนจาก backup 6 วันจาก NAS ของ M2Dev + forensic recovery CMS content ที่สร้างในช่วง backup gap (WhyBuyCards, Zigzag, HeroSplit, FooterColumns, ProductGroups, LobStripeTile) — ตั้งกฎ forbidden command list ป้องกันซ้ำ
- `9 ก.ค.` **Filament FileUpload dehydrated(false) trap** — เกิดซ้ำเป็นครั้งที่ 3 ข้าม session ต้องรวมเป็น pattern กลาง

---

## Week 3 (12–18 ก.ค. 2569) — กำลังดำเนินอยู่

### FE Deploy (DAPP)

- `13 ก.ค.` **Prefetch system + CLS fixes** — commit 14 ไฟล์ (primeHomepage prefetch, preconnect, skeleton height fix) + deploy Coolify staging ทั้ง BE/FE — เจอ Coolify API gotcha: `POST /deploy` 404 แต่ `GET /deploy?uuid=` ใช้ได้จริง

### FFD Portal (education.uficon.com)

- `13-14 ก.ค.` **Grilling pass + sharpened plan** — ยืนยัน 6 จุดตัดสินใจ (role scope, redirect priority, coupon precedence ฯลฯ) เขียนลง Outline
- `14 ก.ค.` **🔴 FFD Portal ล่มบน production 25+ ชม.** — container recreate แล้วดึง image tag เก่า (22 เม.ย., ก่อนมี FFD) ทำให้ SSO/whitelist/coupon หายหมด แก้แล้ว (hotfix + สร้างตาราง DB ที่ขาด เพราะ `docker cp` ไม่ trigger activation hook) — ยังไม่ persistent รอ CI/CD release จริง
- `14 ก.ค.` **Coupon Groups feature ใหม่** — สร้างระบบจัดกลุ่ม ref_code ผูก coupon ร่วมกันเต็มรูปแบบ (schema + admin UI + precedence logic, unit test ผ่าน 5 เคส) ยังไม่ deploy รอทดสอบ UAT

### www.uficon.com

- `14 ก.ค.` **🔴 Payment gateway sprintf() crash (P1)** — ค้นพบ+แก้ bug เดียวกันใน 4 gateway (Bay/KBank QR/SmartPay/SPay) ที่ sprintf() พังเมื่อข้อมูลธนาคารมีอักขระ `%` ปน (PHP 8+ throw ValueError ที่ PHP 7 แค่ warning) — hotfix ขึ้น prod แล้ว (payment_complete() รันก่อน crash เสมอ เงิน/สถานะออเดอร์ไม่เสียหาย) รอ M2Dev commit เข้า git

### Oracle Infra

- `14 ก.ค.` **`/daily-notes` เปลี่ยนเป็น automatic** — จาก on-demand → บันทึกทุกงานย่อยที่เสร็จอัตโนมัติ ละเอียดเทียบเท่า Lark subtask (CLAUDE.md v3.3.0)
- `14 ก.ค.` **Dev Radar log restructure** — เปลี่ยนจากไฟล์ต่อสัปดาห์ → ไฟล์ต่อเดือนแบ่ง sprint รายสัปดาห์ (ไฟล์นี้เอง)

## หมายเหตุ

3 สัปดาห์ล่าสุดมี production incident รวม 4 ครั้ง (DB wipe, FFD Portal revert, payment gateway crash ×1 เจอวันนี้) — ทุกครั้งมี root cause ชัดเจนและแก้แล้ว แต่สะท้อนว่าควรมี automated test/monitoring มากกว่านี้ (checkout ไม่มี automated test coverage มา 4 sessions ติดกันแล้ว, Shield policy gap เกิดซ้ำ 3 ครั้ง)
